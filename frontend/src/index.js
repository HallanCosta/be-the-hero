import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// JSX (JavaScript ML)

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);